/**
 * Manager portal access key. Change this before deploying.
 * Note: any value here is visible in the browser — this only keeps casual visitors out;
 * use server-side auth if the data is highly sensitive.
 */
window.MANAGER_ACCESS_KEY = 'SushiDemure2026';
